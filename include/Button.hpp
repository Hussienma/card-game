#pragma once
#include <SDL2/SDL.h>

#include "UI.hpp"

// class Button: public UI {
// 	private:
// 	public:
// 	Button();
// 	Button(SDL_Color color, SDL_Rect position, GraphicsComponent* graphics, InputComponent* input);
//
// 	void update(Game& game);
// 	void render();
// };
